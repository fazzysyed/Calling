import React, { useState, useEffect } from "react";
import { Launcher } from "react-chat-window";
import firepadRef from "../../config/firebase";
import ChatWindow from "../ChatWindow/ChatWindow";

const Demo = () => {
  return <ChatWindow />;
};

export default Demo;
